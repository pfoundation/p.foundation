// The color mode switch lives at the bottom of every page (see
// src/components/ThemeToggle rendered by src/theme/Footer), not in the
// navbar. Emptying this slot removes the default toggle from both the
// desktop navbar and the mobile sidebar header.
export default function NavbarColorModeToggle() {
  return null;
}
